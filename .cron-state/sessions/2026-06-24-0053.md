# Tick 29 — 2026-06-24 00:24-00:53 PT

**Branch:** main (working directly on main as required)
**Override:** frontend-only batch per Sanjay's 2026-06-23 standing directive.
**Shipped:** 5 / 5 features, all frontend / UX work in `apps/dashboard/`.

## Slices

| # | Slice | SHA |
|---|---|---|
| 1 | findings page `?group=file` collapsible file-grouped sections | 09a4b56 |
| 2 | reviews list sortable columns + active-filter chips | 31ab09a |
| 3 | agent timeline replaces flat agents table on review detail | 6ab294f |
| 4 | Tooltip primitive + 3 wires (theme-toggle / cmdk-trigger / agent status) | 11d1b67 |
| 5 | ShortcutsOverlay (?-triggered cheatsheet) | 4ad0dd4 |

## Gate results

- **Dashboard typecheck:** 5 lines pre-batch baseline = 5 lines post-batch (only the pre-existing `tsconfig.json:18 TS5101 baseUrl deprecation` warning). Verified by checking out tick-28 commit `1f863a1`, running `pnpm --filter @clawreview/dashboard exec tsc --noEmit 2>&1 | wc -l` (= 5), then doing the same on HEAD (= 5).
- **UI:** `pnpm test` -> `passWithNoTests` (no tests exist for the UI primitives package; matches tick 28).
- **Types:** 27/27.
- **Aggregator:** 298/298 (untouched).
- **CLI:** 627/627 (untouched).
- **Telemetry:** passing (untouched).
- **Server:** 408/408 (untouched -- one transient Prisma client flake on the first run resolved cleanly on rerun, unrelated to this batch since no server files were touched).

## Push verification

```
git fetch -q origin && git log --oneline origin/main | head -7
4ad0dd4 dashboard: ShortcutsOverlay (?-triggered cheatsheet) (tick 29)
11d1b67 dashboard: Tooltip primitive + apply to theme-toggle / cmdk-trigger / agents (tick 29)
6ab294f dashboard: agent timeline replaces flat agents table on review detail (tick 29)
31ab09a dashboard: reviews list sortable columns + active-filter chips (tick 29)
09a4b56 dashboard: findings page ?group=file collapsible view (tick 29)
1f863a1 chore(autoship): tick 28 log and session note
f8d0c4d cli: presets diff --on-delta + --on-delta-template (tick 28)
```

## What I learned

- The dashboard had no unit-test infra (no `tests/` dir, no `pnpm test` script wired to vitest). So the gate this tick was the typecheck baseline + ensuring zero new errors. Future frontend ticks should consider adding Playwright e2e fixtures (Playwright is already a devDep in `apps/dashboard/package.json`) for at least the new primitives.
- The Tooltip primitive needed careful SSR-safety in theme-toggle. The mount-time hydration guard means the pre-mount placeholder label reads "theme" so SSR + CSR strings match -- otherwise React 19 throws a hydration mismatch warning.
- The `data-cmdk-trigger` listener walks `closest()` from the click target, so wrapping the trigger button in a Tooltip `<span>` doesn't break the palette open path -- verified by reading the CommandPalette useEffect handler.
- Used `git ls-tree` mental check (the prompt warned `git fetch` can be sticky on this clone) -- but here `git fetch -q origin` + `git log --oneline origin/main | head -1` reported `4ad0dd4` correctly after push, so no force-update needed this tick.
- Adopted the `formatMs` import-survival check after the agent table rewrite: ran `grep -c "formatMs"` to make sure removing the table block didn't strand the import.

## Next tick

Frontend override remains in effect. Backlog refilled with 10 frontend items for tick 30 (keyboard nav on reviews list, sticky filter bar, persisted expand-state, hover preview, sparkline tooltips, virtualized findings, hydration-safe theme bootstrap, copy-link affordance, empty-state CTA, repos page consistency).
