# Tick 13 — 2026-06-21 14:13 PT

5 features shipped, all green.

## Slices

| # | Commit | Slice |
|---|---|---|
| 1 | 7c586eb | `cli: clawreview presets diff --output - (stdout sentinel)` |
| 2 | caaf3af | `cli: clawreview presets diff --base <a> --target <b> (named flags)` |
| 3 | 4cc5b59 | `worker: findingDigest now populates hotspots for dashboard hand-off` |
| 4 | 8558ad9 | `telemetry: clawreview_webhook_stats_window_anchor_total{mode} counter` |
| 5 | 72ae5b7 | `aggregator: computeDigestDrift(persisted, fresh) -> FindingDigestDrift` |

## Theme

Knocked out 5 of the 6 carried/refilled non-dashboard backlog items:
the two presets diff polish items, the worker hotspot hand-off, the
worker drift-detection helper, and the bucketWindow Prometheus
exposition. Dashboard widgets remain the cron-loop blocker (need
live Next.js work outside unit-test-driven cron).

## Gates

- aggregator 216/216 (+11 new)
- telemetry 59/59 (+10 new in metrics.test.ts)
- cli 197/197 (+16 new = 8 sentinel + 8 named flags)
- server 304/304 (+7 new = 2 digest hotspots + 5 webhook stats anchor)
- types 27/27, agents 72/72, diff 24/24, llm 12/12, github 14/14, queue 8/8

**Total: 933 tests passing (+44 over tick 12's 889)**

## Typecheck baseline delta

- `@clawreview/telemetry`: 0 errors (clean)
- `@clawreview/aggregator`: 6 lines pre-existing baseline (node:crypto on fingerprint.ts + diff/context.ts); digest.ts additions clean
- `@clawreview/cli`: 0 errors (clean)
- `apps/server`: 209 lines IDENTICAL to df4536d baseline; zero new errors on touched files

## Push

`72ae5b7` verified on origin/main.

## Notes for tick 14

Five non-dashboard backlog items shipped this tick. The four
dashboard widgets remain the cron-loop blocker. Fresh tick-14
backlog includes: `clawreview review drift <reviewId>` CLI (consumes
this tick's computeDigestDrift), a `/api/reviews/:id/digest`
lightweight DTO (drift-banner without full findings payload),
`clawreview_review_digest_drift_total{kind}` Prom counter, a
findingDigest per-tag bucket, and a presets diff --output - size-check
assertion (refuses bodies over 100KB).

The tick-12 `?bucketWindow=` work and tick-13 anchor-mode counter
together give the dashboard everything it needs for the Live ↔
Snapshot toggle — when that widget lands the postmortem story is
complete end-to-end (UI -> route -> store -> Prometheus).
