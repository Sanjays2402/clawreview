# Tick 35 — 2026-06-26 02:15 PT — FRONTEND BATCH (5 features)

Branch: main (direct commits + push). Frontend override still in effect.
Disk healthy: 14Gi free on / (46% used). node_modules present.

## Shipped (5 slices, 5 commits)

1. **7484793** — Repo detail (/app/repos/[id]) dense restyle. last-review stat +
   recent-reviews `created` column -> LiveRelativeTime; recent-reviews table is a
   `[data-review-row]` focus target with shared ListKeyboardNav (j/k/gg/G + Enter)
   + j/k/open header hint; lowercase/mono restyle of the status/failures/last-review
   stat cards, pause/resume forms, and recent-reviews table; empty state gains
   EmptyStateActions. (+228/-117)

2. **6c54d90** — Installations page dense rewrite (was the last list on the old
   2xl-heading / plain-table styling). PageHeader; `[data-install-row]`
   ListKeyboardNav + hint; Buildings/User type glyph; 24px budget-utilization bar
   on the spent cell (emerald/medium/critical); formatUsd; empty state ->
   EmptyStateActions (install on github -> /login, matching landing-page convention).
   (+110/-46)

3. **3e51000** — Budget repo-health rows clickable -> repo detail
   (/app/repos/<owner>__<repo>); `[data-health-row]` ListKeyboardNav + hint;
   last-review -> LiveRelativeTime; both empty states (no installs, no repos)
   gain EmptyStateActions; focus-visible ring + hover tint. (+66/-15)

4. **3f5e022** — StickyBar opt-in `backToTop` control. Reuses the tick-34 `stuck`
   pin-detection signal to fade in a right-aligned jump-to-top button ONLY while
   pinned; window.scrollTo honors motionScrollBehavior() (prefers-reduced-motion).
   Wired into reviews + repos + SLA tab strips. Default OFF -> existing callers
   keep their exact single-child layout. (+46/-9 across primitive + 3 call sites)

5. **4ab5f8c** — Command palette Tab/Shift-Tab section-jump. Tab -> first row of
   the next section, Shift-Tab -> previous (modular wrap); always preventDefault so
   focus never escapes the modal palette; footer hint + shortcuts-overlay palette
   group document the keys. Pairs with tick-34 arrow wrap-around. (+33/-3)

## Gate

- dashboard `tsc --noEmit`: 1 error line = pre-existing TS5101 baseUrl deprecation;
  ZERO new errors (non-TS5101 count = 0). Identical to tick-34 baseline. Verified
  per-slice as built + once after the final commit.
- `next build`: **"Compiled successfully in 8.1s"** — all 5 slices compiled GREEN
  into the production bundle. Build then fails ONLY in Next's separate standalone
  type-check phase on root `src/app/layout.tsx:5 import './globals.css'` — proven
  pre-existing AND untouched (import line byte-identical origin/main vs HEAD; root
  layout absent from the batch diff). Documented environmental quirk, identical to
  ticks 31/33/34.
- Scope: 9 files, all under apps/dashboard/src/ (+333/-133). Zero backend/packages
  touched.
- Push verified: origin/main -> 4ab5f8c.

## Notes for next tick (36)

- Backlog refilled with 10 frontend-first items (per-row hover preview, trends
  severity deep-links, constraint-aware windowed findings paint, more empty-state
  CTAs, agent-timeline first-Tab hint, bare-page `gg` scroll-to-top parity, theme
  flash audit, copy-link toast, repo-detail sparkline, installations detail restyle).
- The per-row hover preview keeps being carried because it needs a server DTO add
  (`bySeverity` on ReviewListItem) — slightly outside the pure-frontend loop but
  doable with a minimal wired-straight-to-the-UI backend touch (the override allows
  that "only when a frontend feature truly needs it").
- Backend roadmap (worker blame, dashboard-for-counters wiring, Grafana JSON,
  presets/filter-report hooks) stays parked under the standing override.
