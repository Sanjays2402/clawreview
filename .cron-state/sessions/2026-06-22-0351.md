# Tick 17 — 2026-06-22 03:51 PT

5 features shipped on `main`. All gates green, all 1129 tests passing.

## Features

| # | Slice | SHA |
|---|---|---|
| 1 | Aggregator `findingDigest` topAuthors slice (opts.blame + opts.topAuthors) | 29368c0 |
| 2 | Server `/api/reviews/:id/digest ?slim` accepts comma-separated field list | e9e561b |
| 3 | Telemetry `clawreview_review_drift_watch_polls_total{result}` counter + CLI wiring | f7f1e67 |
| 4 | CLI `review drift --watch --on-drift-template slack\|webhook` | 00fe514 |
| 5 | CLI `presets diff --since-range <a>...<b>` triple-dot symmetric-diff | 348d5e9 |

## Gate results

| Package | Tests | Δ |
|---|---|---|
| aggregator | 243 | +7 (topAuthors group) |
| telemetry | 93 | +9 (derive + observe watch-poll) |
| cli | 312 | +26 net new |
| server | 324 | +5 (slim-fields) |
| types | 27 | — |
| agents | 72 | — |
| diff | 24 | — |
| llm | 12 | — |
| github | 14 | — |
| queue | 8 | — |

**Total: 1129 tests passing (+47 over tick 16's 1082).**

## Notes

- Disk pressure remained at 99% on / for the entire tick. Used `/Volumes/Projects/` for commit messages and TMPDIR; `write_file` for source edits where `patch` would be risky. No stderr-into-source pollution observed this tick.
- Added `@clawreview/telemetry` as a workspace dep on `apps/cli` (needed for the watch-loop metric wiring). pnpm install ran cleanly.
- Server tests are slow to warm up (~30s for first hook); used `--hookTimeout 30000` to avoid spurious timeouts. Once warm, run in ~15s.
- All 5 features are from the seeded backlog #6-10 (the unit-test-driven track). The 5 dashboard / blame-attribution items still need work outside the cron loop.
- Five backlog items refilled for tick 18 (one of them is the natural "worker server-side blame wiring" follow-up to today's topAuthors slice).

## Push verification

```
$ git fetch -q origin && git log --oneline origin/main | head -5
348d5e9 cli: presets diff --since-range <a>...<b> (triple-dot symmetric-diff)
00fe514 cli: review drift --watch --on-drift-template slack|webhook (sugar on --on-drift)
f7f1e67 telemetry: clawreview_review_drift_watch_polls_total{result} counter + CLI wiring
e9e561b server: /api/reviews/:id/digest ?slim accepts comma-separated field list
29368c0 aggregator: findingDigest topAuthors slice (opts.blame + opts.topAuthors)
```

All 5 commits on origin/main.
