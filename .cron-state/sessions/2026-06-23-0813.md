# Tick 25 — 2026-06-23 08:13 PT

## Summary
Shipped 5 features in a clean batch, all on `main`, pushed to `origin/main`.

## Features
1. **Server `/api/reviews/:id/filter-report ?fields` deny-list (-prefix)** — `a9f8a26`
   - Minus-prefix complement to tick-24 allowlist
   - 7 new tests (deny-list group)
2. **CLI `stats --jsonl --no-footer`** — `52f4d86`
   - Suppress JSONL footer line (6-line stream variant)
   - 4 new tests
3. **CLI `review filter-report --watch --on-applied-change <cmd>` + `--on-applied-template`** — `55d5bd2`
   - Hook fires on applied-bit transition; template form mirrors tick-17/19/24 patterns
   - 19 new tests (9 hook + 10 template)
4. **CLI `review filter-report --diff <baseId> <targetId>` two-review compare** — `066c389`
   - parseFilterReportDiffConfig + FilterReportDelta + computeFilterReportDelta + runner
   - 12 new tests (8 integration + 4 pure helpers)
5. **Telemetry `clawreview_review_filter_report_diff_total{result}` counter** — `a88956f`
   - Closed identical|delta|error label set; CLI wiring on each path
   - 12 new tests (9 telemetry + 3 wired CLI)

## Gate results
- **Total tests: 1471 (+54 from tick 24's 1417)**
- telemetry: 129 (+9)
- cli: 509 (+38)
- server: 401 (+7)
- aggregator/types/agents/diff/llm/github/queue: unchanged baselines
- All typecheck deltas zero; server's tsc line count IDENTICAL to baseline (215)

## Push verified
`git fetch -q origin && git log --oneline origin/main | head -1` → `a88956f`

## Backlog refill
- 5 of 13 tick-25 backlog items shipped (5 fresh + 1 from carried)
- 7 carried items still need dashboard / external-integration work
- Refilled with 5 fresh items for tick 26: --on-delta hook + template, --json-stream, ?fields=* star sugar, --output, diff-duration histogram

## Notes
- Combined features 3 & 4 (hook + template) into one commit because the
  template is the natural pair of the hook (mirrors tick-17/19/24's
  `--on-drift-template` / `--on-recover-template` / `--on-regression-template`
  conventions). Each one alone would have been ~half the slice; together
  they make a single complete capability.
- All commits use cron identity `Cake (cron) <51058514+...>`.
- No emojis in git artifacts.
