# Tick 50 — 2026-06-28 23:08 PT — FRONTEND BATCH

5/5 frontend slices, scoped to apps/dashboard/src/. Drained tick-50 backlog.

1. **87447bc** sla-breaches-table.tsx — sticky thead parity (was translucent + non-sticky; now sticky top-0 z-10 opaque bg-bg-subtle + border, matching reviews/agent/trends)
2. **0897e43** app/layout.tsx — running dot + failed badge coexist (dim dot rides beside badge on a mixed fleet; failed still wins the deep-link)
3. **7f94ad9** toaster.tsx — shift+u undo-all fires a single non-actionable "n undone" confirmation (no action -> no loop)
4. **43d0a2b** reviews/page.tsx — anomaly tab title folds "N both" (spike AND above-baseline overlap; predicates extracted so it can't drift)
5. **55a7059** command-palette.tsx — go-to-list gains an "enter" kbd hint (closes mouse<->kbd discoverability loop)

Gate: tsc 0 new errors (TS5101 baseline only). build Compiled successfully 4.3s; standalone-typecheck fails ONLY on untouched root globals.css quirk. Disk 83%. Pushed f29b1db..55a7059. Refilled 10 fresh frontend items for tick 51.
