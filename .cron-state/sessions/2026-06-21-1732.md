# Tick 14 — 2026-06-21 17:32 PT

Shipped 5 feature slices on `main` and pushed:

1. **a37bb91** — aggregator: `findingDigest` byTag bucket + per-tag drift delta.
   `UNTAGGED_BUCKET = '(untagged)'` sentinel; multi-tag findings contribute to
   N buckets; computeDigestDrift gains byTagDelta. 11 new tests on
   `packages/aggregator/tests/digest.test.ts` (5 byTag + 6 drift).

2. **d3ac2cf** — server: `GET /api/reviews/:id/digest` DTO returning
   `{ persisted, fresh, drift }`, plus `clawreview_review_digest_drift_total{kind}`
   counter (closed `['fresh','stale']` set). Legacy persisted=null
   synthesises an empty digest so drift surfaces as "every fresh bucket
   is positive". 14 new tests across telemetry (9) and the server route (5).

3. **211cc6f** — cli: `clawreview review drift [--input <path>] [--format text|json]`.
   Consumes either /api/reviews/:id or /api/reviews/:id/digest body shape,
   computes drift locally with the same caps the worker uses (8/8 +
   hotspots:true), exits 3 on drift (mirrors `presets diff` CI-gateability).
   12 new tests in `apps/cli/tests/review.test.ts`.

4. **1438252** — cli: `clawreview presets diff --max-output-bytes <n>`
   size cap (default 100 KiB, 16 MiB ceiling, 0 disables). UTF-8 byte
   counting via Buffer.byteLength; stdout / file paths get different
   stderr hints. 22 new tests: 11 parser + 6 enforcer + 5 integration.

5. **388625d** — cli: `clawreview presets diff --since <git-ref>`.
   Chain A's local presets resolve at the ref via gitShow + git ls-tree;
   chain B stays at HEAD. `loadLocalPresetsAtRef(cwd, ref, loadFile?, listEntries?)`
   takes injectable loaders so unit tests stub git out. Echoes ref in
   JSON / YAML header / text header. 14 new tests (7 unit with
   stubbed loaders + 7 integration with a real bootstrapped git repo).

## Gate

All ten package test suites green:

| Package | Tests | Δ vs tick 13 |
|---|---|---|
| aggregator | 225 | +9 |
| telemetry | 68 | +9 |
| cli | 245 | +48 |
| server | 309 | +5 |
| types | 27 | 0 |
| agents | 72 | 0 |
| diff | 24 | 0 |
| llm | 12 | 0 |
| github | 14 | 0 |
| queue | 8 | 0 |
| **total** | **1004** | **+71** |

Touched-package typecheck delta (HEAD vs baseline):
- `@clawreview/telemetry`: 0 errors (clean).
- `@clawreview/cli`: 0 errors (clean).
- `@clawreview/aggregator`: 6 errors (IDENTICAL to baseline; pre-existing
  `node:crypto` in fingerprint.ts and `node:fs/promises` / `node:path`
  in diff/context.ts).
- `apps/server`: 209 lines (IDENTICAL to d247294 baseline). The new
  `/digest` route imports `findingDigest` / `computeDigestDrift` /
  `observeReviewDigestDrift` cleanly; the FastifyInstance / pino /
  api-auth / rate-limit / webhooks / server / worker baseline noise
  is unchanged.

## Carried backlog for tick 15

Six fresh follow-ups seeded plus the four perennial dashboard items
(stats / recent / operator-poll / blame attribution) -- all need
non-unit-test work (Next.js page wiring, GitHub API blame plumbing).

The tick-14 features stack cleanly on tick-12's persisted digest +
tick-13's computeDigestDrift: the byTag bucket flows through both
the persisted digest (since the worker uses `findingDigest` for
persistence) and the drift report; the /digest route + review drift
CLI are the read-side consumers that close the loop.

## Disk pressure

System root volume / still at 98% (was 100% earlier ticks). One
`patch` mode write to metrics.ts post-write-verify failed with a
snapshot read-back mismatch -- the edit itself landed cleanly,
verified via LSP diagnostics + tsc. Used `write_file` for the
larger config.ts edit; both succeeded.

Lock released. Pushed `388625d` to origin/main, verified via
`git ls-remote origin main`.
