# Tick 32 — 2026-06-25 12:05 PT — FRONTEND BATCH (5 features)

Standing frontend override active (set 2026-06-23). All 5 slices are user-facing
UI/UX work in `apps/dashboard/src/`. Disk healthy (16Gi free on /), on `main`,
started at origin HEAD 8f5bc42.

## Shipped (one commit per slice, cron identity, no emoji)

1. **01b3ec0** — trends sparkline hover-scrub + shared dayLabels helper
   - Swapped trends findings/day chart from static `Sparkline` to
     `InteractiveSparkline` (hover hit-targets, dot, dashed guide, edge-flipping
     readout, keyboard cursor) so trends matches the overview's interactivity.
   - Extracted the duplicated oldest-first `dayLabels(n)` into `lib/format.ts`;
     overview + trends now share it (overview local copy removed).

2. **289c044** — sla breaches severity tabs + sortable columns + filter chips
   - New `components/sla/sla-breaches-table.tsx`: severity filter tabs with live
     count badges (`?sev=`), sortable severity/age/sla/overdue columns (aria-sort
     + arrow, severity via rank map), active-filter chips + clear-all,
     `useListKeyboardNav` over `[data-sla-row]`, dense lowercase/mono/sev restyle.
   - Policy-override query keys (critical_hours, ...) captured into a
     `policyParams` map and threaded through every filter/sort link so a custom
     remediation window survives a tab click or column sort.
   - Pure helpers exported: filterSlaBreaches / sortSlaBreaches /
     countSlaBySeverity / parseSlaSort / parseSlaDir / parseSlaSeverity.

3. **e83d068** — command palette keeps active row scrolled into view
   - Per-row callback-ref map keyed by flat index; `scrollIntoView({block:
     'nearest'})` on idx change (and when the filtered set shifts) so arrow-key /
     ctrl-n/p nav keeps the highlighted row visible on the now-longer
     recent-review list.

4. **c9070e2** — live-refreshing relative timestamps on reviews + audit
   - New `components/ui/live-relative-time.tsx`: re-formats `formatRelative` on a
     30s interval, IntersectionObserver-gated (64px rootMargin) so off-screen
     rows don't churn; SSR-safe + `suppressHydrationWarning`; semantic `<time
     dateTime>` with absolute local time as title.
   - Wired into reviews `created` column + audit `when` column (kept inside the
     existing `[data-audit-row]` focus target so keyboard nav is unchanged).

5. **ff9e096** — honor prefers-reduced-motion across animations + smooth scroll
   - `globals.css` `@media (prefers-reduced-motion: reduce)` block collapses every
     animation (fade-in, shimmer, agent-scrub, tooltip, palette) and transition to
     one near-instant frame (!important over Tailwind utils) + forces
     `scroll-behavior: auto`.
   - JS-driven smooth scroll bypasses CSS, so new `lib/motion.ts`
     (`prefersReducedMotion()` SSR-safe matchMedia probe + `motionScrollBehavior()`);
     finding deep-link `scrollIntoView` now jumps instantly under reduced motion.

## Gate (run ONCE at end of batch)

- `pnpm --filter @clawreview/dashboard exec tsc --noEmit` output IDENTICAL to the
  tick-31 baseline: the ONLY line is the pre-existing TS5101 `baseUrl` deprecation
  on tsconfig.json:18. ZERO new type errors. Verified per-slice as built and once
  more after the final commit (`grep -c "error TS"` = 1).
- `next build` **compiled the production bundle successfully** ("Compiled
  successfully in 12.3s") — all 5 slices green. It then fails ONLY in Next's
  separate standalone type-check phase on `layout.tsx:5 import './globals.css'`
  (CSS side-effect module-declaration quirk). PROVEN pre-existing: line 5 is
  byte-identical on origin/main and HEAD, and my batch never touched layout.tsx.
- Scope clean: 12 files, +536/-105, all under `apps/dashboard/`. Zero
  backend/packages touched (`git diff --name-only origin/main...HEAD | grep -v
  '^apps/dashboard/'` = empty).
- Push verified: `git fetch -q origin && git log --oneline origin/main | head -1`
  -> ff9e096.

## Notes / carry-forward

- 5 of 10 tick-32 backlog items shipped; 5 carried + 5 fresh seeded for tick 33
  (sticky filter bars, per-row hover preview, findings virtualization,
  empty-state CTAs, agent-timeline scrub hint, repos live-time, trends sev
  deep-links, palette wrap-around, SLA keyboard hint, findings sticky strip).
- The static `Sparkline` UI primitive is now unused by the dashboard pages
  (overview + trends both use InteractiveSparkline); it remains exported from
  `@clawreview/ui` for decorative cases — left in place intentionally.
- Backend roadmap stays parked under the frontend override until Sanjay flips it.
