# Tick 2 — 2026-06-20 02:53 PT

Five features shipped on `feature/autoship` (cc027d6..fa12f29):

1. **`5c6c2b7`** feat(aggregator): reviewdog rdjsonl exporter + CLI `--format rdjsonl`
2. **`eda6c4e`** feat(cli): `.clawreviewignore` project-level path filter
3. **`0517234`** feat(aggregator): hotspot detection + PR comment Hotspots block
4. **`58eb06b`** feat(agents): per-language prompt rules injection in PromptedAgent
5. **`fa12f29`** feat(cli): `clawreview explain <fingerprint>` command

## What worked

- Five vertical slices, one commit each, each commit independently revertible
  and shipped with focused tests in the affected package(s). No drive-by
  refactors, no padding.
- The per-language rules injection ships behind an `injectLanguageRules` flag
  (default `true`) so PromptedAgent's existing behaviour is preserved unless
  the agent opts out. Tests assert both opt-in and opt-out paths via
  MockProvider.history.
- The hotspots feature lives in its own module (`hotspots.ts`) plus a thin
  wiring point in `comment.ts`, so the algorithm is reusable (CLI text
  rendering, dashboard view) without needing the renderer.

## What I had to push through

- `/tmp` and `/var/folders/.../T` are both on the full system root volume
  (228 GiB used, 115 MiB free). Hermes' shell snapshot scripts fail every
  command with `No space left on device` stderr, and the `patch` tool was
  splicing that stderr into source files (corrupted three files mid-tick).
  Switched entirely to `write_file` for source edits and wrote all commit
  messages under `/Volumes/Projects/.cron-msg.txt` instead of `/tmp/msg.txt`.
  Documented both workarounds in STATE.md.
- Slice 4 needed `@types/node` added to `packages/agents/package.json` for
  the loader's `node:fs`/`node:url` imports to typecheck. Added it cleanly;
  did not touch other baseline-red packages.

## Gate results

| Package | Tests | New | Status |
|---|---|---|---|
| aggregator | 117 | +29 (rdjsonl 10, hotspots 15, comment +4) | green |
| agents | 49 | +12 (loader 8, prompted-agent +4) | green |
| cli | 36 | +21 (ignorefile 9, explain 12) | green |
| diff | 24 | 0 | green |
| types | 7 | 0 | green |
| llm | 12 | 0 | green |
| github | 14 | 0 | green |
| queue | 3 | 0 | green |
| telemetry | 6 | 0 | green |
| server | 179 | 0 | green |
| **Total** | **447** | **+62** | **green** |

Touched-package typecheck: `@clawreview/cli` clean, `@clawreview/aggregator`
and `@clawreview/agents` red only on the documented baseline (`@types/node`
missing on cross-package deps like `packages/diff/src/context.ts`,
`packages/llm/src/registry.ts`, `packages/aggregator/src/fingerprint.ts`).
Zero new typecheck errors from this tick.

Verified `git ls-remote origin feature/autoship` -> `fa12f29`.

## Next tick handoff

10 roadmap items remain (#8, #10, #11, #12, #14, #15, #17, #18, #19, #20).
Best candidates for tick 3:

- `#19` Per-agent latency histogram — packages/telemetry already has the
  Prometheus surface, just need a histogram + plumbing from pipeline.ts's
  onAgentEnd callback.
- `#18` Queue introspection endpoint — packages/queue has the adapter iface;
  just need an InMemoryQueue.stats() and a /internal/queue Fastify route.
- `#17` `clawreview diff-stats` — small CLI slice mirroring `explain`, uses
  packages/diff parseUnifiedDiff and language detection only (no LLM call).
- `#10` Per-author finding breakdown — needs git blame integration in cli
  or aggregator; touch with care because it adds a new IO path.
- `#15` Cost-budget pre-flight — packages/llm already has a token counter
  for usage; we'd add a heuristic estimator over diff size + system prompts.

The "Foundational infra fix" backlog item (wire `@types/node` into the
remaining baseline-red packages) is still pending and worth doing once
the system disk pressure clears — would let `pnpm typecheck` and
`pnpm build` go green at the root and re-enable turbo's `^build` test
cascade.
