# Tick 7 — 2026-06-20 19:44 PT

Branch: `feature/autoship`
Cron identity: `Cake (cron) <51058514+Sanjays2402@users.noreply.github.com>`
Lock: `/tmp/clawreview-cron-tick.lock` (released at end of tick).

## Five features shipped

| # | Slice | SHA |
|---|---|---|
| 1 | `severity_rules` confidence band matchers (`min_confidence` + `max_confidence`) + new `drop: true` action | 345f712 |
| 2 | `clawreview presets list` CLI (built-in + local with declared extends chain, shadowing annotation) | a10b6cb |
| 3 | `clawreview lint-config --fix` (curated AST-level rewrites for severity_threshold / comment_style / inline_comments.min_severity typos; preserves comments + key order) | a1f48c3 |
| 4 | Telemetry `clawreview_findings_dropped_total{reason}` counter with closed reason set + worker wiring for all three drop sources (severity_rule, min_confidence, inline_suppression) | 3b80acf |
| 5 | Webhook stats endpoint gains `granularity` (minute/hour/day) + per-granularity bucket caps + modern `buckets` query knob | 4112f6c |

## Gate results

- types       27/27   (+5 new on min_confidence + severity_rules confidence/drop refinement)
- telemetry   29/29   (+5 new on findings_dropped_total: closed-reason guarantee, per-reason emission, defaults, accumulation, safety no-op)
- aggregator  172/172 (+7 new severity-rules drop + confidence band cases)
- cli         93/93   (+12 new = 6 presets list + 6 lint-config --fix)
- agents      72/72
- diff        24/24
- llm         12/12
- github      14/14
- queue       8/8
- server      205/205 (+5 new granularity cases on /api/internal/webhook/stats)

**Total: 656 tests verified passing (+22 over tick 6's 634).**

Typecheck delta: zero new errors introduced. All red lines remain on the documented baseline list (`node:crypto`/`node:fs/promises` missing types on packages/aggregator/fingerprint.ts + packages/diff/context.ts, pino resolution on apps/server/worker.ts, FastifyInstance/Logger mismatch on apps/server/{api-auth,rate-limit,webhooks,server}.ts).

## Push verification

```
$ git ls-remote origin feature/autoship
4112f6c1de9470d5fa484975ce2eaa2e7e212f6c   refs/heads/feature/autoship
```

Local `refs/remotes/origin/feature/autoship` advanced via `git update-ref` after the fetch quirk noted in STATE.md.

## Backlog status

Tick-7 incoming backlog had 6 items. 3 shipped this tick (#1 = `severity_rules` confidence matchers, #2 = `presets list`, #3 = `lint-config --fix`). 3 remain open and are carried into tick 8:
- Worker-side blame attribution + `clawreview_authors_attributed_total` wiring (needs GitHub API integration, outside the unit-test-driven loop).
- Dashboard widgets for `/api/internal/webhook/stats` + `/recent` cursor pagination (Next.js wiring, outside the per-package test loop).

Refilled with 4 fresh items for tick 8 covering aggregator helper extraction, CLI grouping flags, rate-limit class for operator polling endpoints, and `clawreview presets show <name>` as a follow-up to this tick's `presets list`.

## Notes / lessons

- The earlier write_file lint warnings about pre-existing `node:*` baseline errors are still cosmetic noise; the production build still passes via vitest's own resolution path.
- `appliedFilters.hourBuckets` -> `appliedFilters.buckets` rename in /webhook/stats is a tiny breaking-shape change but only the test file referenced it (no production consumer yet); updated in the same commit.
- Per-granularity bucket caps live in the store, not the route, so a future direct caller (worker, CLI) gets the same protection without re-validating.
- All five commits are small, surgical, and revertible. No cross-cutting "infra" changes this tick; the touched-package delta stays minimal.
