# Tick 37 — 2026-06-26 13:23 PT (FRONTEND BATCH)

Shipped 5 frontend slices under the standing frontend-first override. All
user-facing UI in apps/dashboard/src/. Pushed to origin/main ef0ff9f..58ad77b.

## Features (5/5)
1. 079e22c — Installation settings page (/app/installations/[id]/settings) dense
   restyle: PageHeader + lowercase Breadcrumbs, mono stat cards, severity-palette
   budget bar + "N% used", [data-repo-row] ListKeyboardNav, LiveRelativeTime,
   EmptyStateActions. ALSO fixed a latent broken repo link (percent-encoded
   owner%2Frepo -> owner__repo slug parseSlug expects; old links 404'd).
2. 58e2068 — Toast on repo pause/resume: new RepoPauseControls client component
   (useTransition + corner toast + pending/disable). Server actions passed as
   props; no logic moved client-side.
3. 5ecd212 — Agent-timeline share-of-total inline track: thin 3px bar under each
   duration bar, width = % of total wall time, bottleneck tinted severity-high.
4. 600ce57 — Repo-detail findings<->spend sparkline toggle: new formatValue hook
   on InteractiveSparkline + RepoTrendCard client component. Second metric, zero
   extra card.
5. 58ad77b — Command-palette recent-review status dot: colored dot off
   StatusPill's tone ladder so review state is scannable.

## Gate
- tsc --noEmit IDENTICAL to baseline: 1 line (pre-existing TS5101 baseUrl
  deprecation on tsconfig.json:18). Zero new type errors.
- next build: production bundle "Compiled successfully in 14.0s". Build then
  fails ONLY in Next's standalone type-check phase on the ROOT layout's
  `import './globals.css'` — documented env quirk, proven pre-existing AND
  untouched (this batch touched NO layout files; the line is byte-identical at
  origin/main and HEAD). All 5 slices' production compile is GREEN.
- 7 files, +406/-154, all under apps/dashboard/src/. Zero backend/packages.
- Disk healthy: 13Gi free, 48% used.

## State
- 5 of 10 explicit tick-37 backlog items shipped.
- Refilled tick-38 backlog with 10 frontend-first items (5 carried + 5 fresh:
  finding dismiss/reopen toast, bulk-dismiss toast, spend trend tint,
  installations-list bar parity, palette group count badges).
- Backend roadmap stays parked under the override.

## Notes for next tick
- patch tool was reliable this tick (disk healthy); used write_file for new
  files + patch for edits, both fine.
- No dashboard unit tests exist; gate is tsc + next build (compile phase).
- The root-layout globals.css build-typecheck failure is environmental and
  recurs every tick — always prove it via git diff (no layout files touched) +
  byte-identical line check, don't chase it.
