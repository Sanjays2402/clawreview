# Tick 16 session — 2026-06-22 00:03 PT

Five clean feature slices shipped to main as separate, revertible
commits. Theme: complete the "drift health + presets range" backlog
from tick 15 by wiring the three remaining surfaces and polishing
two operator ergonomics gaps.

## Commits (in order)

1. **09e1eb4** `comment: topTags "By tag" breakdown line + worker wiring (cap 8)`
   - `renderPrComment` gains `topTags?: number` opt mirroring the
     existing `topCategories` / `topAgents` shape.
   - Worker passes `topTags: 8` so PR comment header now carries the
     same per-tag ordering the dashboard reads from `digest.topTags`.
   - Three breakdown lines (category / agent / tag) share `findingDigest()`
     end-to-end. Tag-free callers see no change (opt-in by default).
   - Tests: 5 new in comment.test.ts.

2. **e293dfa** `cli: clawreview presets diff --since-range <a>..<b> (git-style range sugar)`
   - Splits `<a>..<b>` into chain-A / chain-B refs.
   - Mutex with --since / --since-base / --since-target (refuses
     combos as typos with per-flag error messages).
   - Pure `parseSinceRange()` helper with closed error sentinels
     for triple-dot, multi-separator, empty-base, empty-target.
   - JSON / YAML / text headers echo the original `sinceRange`.
   - Tests: 13 new (7 pure + 6 integration).

3. **f2d4835** `server: /api/reviews/:id/digest ?slim=true projection (strips full sparse maps)`
   - `?slim=true` strips byTag / byCategory / byAgent / byFile
     from both `persisted` and `fresh` payloads.
   - Preserves totals + top-N slices + hotspots + drift summary.
   - Composes with `?recompute=cached`. Legacy reviews keep
     `persisted: null` on slim mode.
   - `slimDigest()` pure helper exported via the route barrel.
   - Tests: 4 new in reviews-route.test.ts.

4. **74b04a8** `telemetry: deriveReviewDigestPersistedDriftLogLevel + worker warn elevation`
   - New pure predicate: `stale -> warn`, `unchanged -> info`,
     `fresh -> none`.
   - Worker dispatches off the returned level; stale-between-runs
     now hits `log.warn` so on-call alerts fire without scraping
     Prometheus.
   - Tests: 5 new (every kind + closed-set + composition with kind helper).

5. **389fba0** `cli: review drift --watch --on-drift <cmd> [--on-drift-once] (drift hook)`
   - Exec `<cmd>` with JSONL drift payload on stdin when a poll
     surfaces drift.
   - `--on-drift-once` gates to first transition only (robust to
     clean -> drift on poll 2).
   - Hook failure surfaces on stderr but doesn't abort the loop.
   - Injectable `WatchOnDriftExecer` for tests.
   - Tests: 8 new in review.test.ts --on-drift group.

## Gate results

| Package | Tests | Δ vs tick 15 |
|---|---|---|
| types | 27/27 | 0 |
| diff | 24/24 | 0 |
| llm | 12/12 | 0 |
| agents | 72/72 | 0 |
| aggregator | 236/236 | +5 |
| queue | 8/8 | 0 |
| github | 14/14 | 0 |
| telemetry | 84/84 | +5 |
| cli | 286/286 | +21 |
| server | 319/319 | +4 |
| **Total** | **1082/1082** | **+25** (gross) / +35 (per-feature sum) |

Typecheck delta verified: server tsc output line count IDENTICAL
to origin/main baseline (213 lines pre-tick-16 vs 213 lines post)
— zero new errors. Telemetry + CLI both fully clean. Aggregator
red only on the pre-existing `node:crypto` / `node:fs/promises`
baseline (touched files clean).

Push verified: `git fetch -q origin && git log --oneline origin/main | head -1` -> `389fba0`.

## Backlog refill

Tick 17 backlog seeded with 5 fresh items:
- CLI `--on-drift-cmd-template slack` sugar
- Server `?slim=byTag,byAgent` field-list form
- Aggregator `findingDigest` topAuthors slice
- CLI `presets diff --since-range a...b` symmetric-difference
- Telemetry `clawreview_review_drift_watch_polls_total{result}`

Five carried follow-ups (4 dashboard wiring + 1 blame-attribution)
still need work outside the unit-test-driven cron loop.

## Notes

- The system disk is still at 90% (down from 100% last few ticks) —
  /tmp + Hermes snapshot writes now succeed, but I kept the workflow
  pinned to /Volumes/Projects/ for safety (TMPDIR, commit-message
  files, typecheck snapshots all live there).
- Used `write_file` exclusively for source edits and the small `patch`
  edits for delta-style replacements. Both worked cleanly today.
- The aggregator `byTag` field was already populated on FindingDigest
  since tick 14; today's comment wiring was a pure rendering addition.
- The `slim` projection was originally scoped as `?include=topTags`
  in the tick-15 backlog seed; promoted to the more general
  `?slim=true/false` because the all-or-nothing form covers every
  dashboard panel that just needs ranked breakdowns. The
  comma-separated form (`?slim=byTag,byAgent`) is now seeded for
  tick 17 as the natural extension.
