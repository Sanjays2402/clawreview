# Tick 27 — 2026-06-23 15:49 PT

## Shipped (5 features)

| # | Slice | SHA |
|---|---|---|
| 1 | Aggregator `computeFilterReportDelta` + `FilterReportBodyLike` (extracted from CLI; CLI delegates) | 9214639 |
| 2 | CLI `review filter-report --diff --on-delta-once` (process-level dedup) | 9308fb4 |
| 3 | CLI `review filter-report --diff --max-output-bytes <n>` (size cap; default 100 KiB) | dee94d6 |
| 4 | CLI `review filter-report --diff --input <path\|->` (skip HTTP; read envelope from file/stdin) | 8aae7cc |
| 5 | Server `clawreview_review_filter_report_reads_projection_total{projection}` counter (full\|slim\|fields) | 5aa5b04 |

## Gate

- aggregator: 286/286 (+11 filter-report-delta)
- telemetry: 145/145 (+10 = 5 derive + 5 observe)
- cli: 580/580 (+44 = 10 on-delta-once + 17 max-output-bytes + 17 --input)
- server: 408/408 (+2 projection counter route)
- types/agents/diff/llm/github/queue: unchanged
- **total 1617 (+108 over tick 26's 1509)**

Typecheck: telemetry / cli clean (0 errors); aggregator red only on baseline (filter-report-delta.ts adds zero); server line count IDENTICAL to tick-26 (215 lines pre vs post).

Push verified: `git log origin/main | head -1` -> `5aa5b04`.

## Notes / lessons

- The aggregator mirror (F5) had to use a STRUCTURALLY-typed `FilterReportBodyLike` (not the CLI's `FilterReportBody` union) so apps/cli isn't a dependency of `@clawreview/aggregator`. The CLI's tick-25 `FilterReportBodyFull` / `FilterReportBodySlim` are assignable to it -- delegation is one line.
- `--on-delta-once` cache lives in a module-level `Set` keyed by `${baseId}|${targetId}`. Process-lifetime by design; `resetOnDeltaOnceCache` is a test seam. Failing hooks STILL record the key (operator asked for "once per process", not "until success"; retries need a fresh process).
- The `--input` flag flips `--server` from required to optional. Parser change is one bit; the runner branches before the HTTP fetch. Stdin sentinel mirrors `--output -`'s symbol pattern.
- `clawreview_review_filter_report_reads_projection_total` was the cleanest framing of the "peak-bucket metric" backlog item -- adds a new orthogonal axis instead of just measuring shape distribution. The cross-series invariant (tick-23.full == tick-27.full + tick-27.fields) is pinned in the telemetry test suite so a future refactor that breaks reconciliation surfaces immediately.
- Tests had two near-misses fixed mid-tick: (1) `runDiffWithHook`'s id-includes-other-id collision (need non-overlapping base/target ids when both share a substring), (2) pretty-printed JSON output uses `"hasDelta": true` (with space) not `"hasDelta":true`. Both caught + fixed in the same edit cycle.
- Final commit identity verified: `Cake (cron) <51058514+Sanjays2402@users.noreply.github.com>` on every commit. Direct push to `main` succeeded; no PR.
