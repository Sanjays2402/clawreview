# Tick 46 — 2026-06-28 ~06:00 PT

**Branch:** main (direct, per standing autoship rules). Frontend override ACTIVE.
**Pushed:** `0401729..217e4fb` on origin/main. Verified landed.

## Shipped 5 frontend slices (all apps/dashboard/src/, +205/-25, 4 files)

1. **52e21dc** — Reviews list multi-status `?status=failed,running` filter.
   parseStatusList (in-file pure, deduped/validated/canonical-ordered).
   1 status = old server filter byte-for-byte; 2+ = fetch unfiltered, OR-filter
   client-side. Tabs become multi-select (dots, toggle membership, `all` clears);
   per-status removable chips. Commas literal in URL.
2. **38256d9** — Overview reliability "N need attention" combined deep-link to
   the new comma-status list. Shown only on a genuine failed+running mix.
3. **8f0df49** — Overview SLA rows: oldest-in-view age brightening (text-fg bump,
   distinct from critical overdue tint), parity with the full table's tick-45 idiom.
4. **9373ea4** — Findings filter-strip "windowed" badge when content-visibility
   paint-deferral is active (60+ findings); title reassures all rows present + nav-able.
5. **217e4fb** — Command-palette help-panel g-prefix cheat-sheet: live grid of real
   `g x` chords from ROUTES' hint fields (can't drift), replacing the prose.

## Gate
- tsc: 2 lines, byte-identical to baseline (only pre-existing TS5101); 0 new errors.
- next build: production bundle "Compiled successfully in 5.1s". Residual failure is
  ONLY the documented globals.css standalone-typecheck quirk (layout.tsx untouched,
  failing line byte-identical origin/main vs HEAD; identical to ticks 31-45).
- Diff scope: 4 files, all apps/dashboard/src/, zero backend/packages leak.

## In-gate catch + fix (worth remembering)
- First `next build` FAILED: slice 1 had `export function parseStatusList` and
  Next.js page files reject arbitrary named exports ("not a valid Page export
  field"). Fix: dropped `export` (helper is in-file only). Folded into slice 1 via
  `git commit --fixup` + `GIT_SEQUENCE_EDITOR=: git rebase -i --autosquash origin/main`
  so each slice stays self-contained + revertible. Rebuild passed the page-export phase.
- LESSON: `tsc --noEmit` does NOT catch the Next page-export constraint -- only
  `next build`'s type-check phase does. ALWAYS run next build before pushing, not just tsc.

## Environment gotcha (cost me two cleanups)
- write_file / patch with a RELATIVE path inherit the terminal's last `cd`. After
  `cd apps/dashboard`, a write to `apps/dashboard/src/...` landed in a NESTED
  `apps/dashboard/apps/dashboard/src/...` dup. Had to cp the content out + rm -rf
  the stray dir. FIX GOING FORWARD: always pass ABSOLUTE paths to write_file/patch,
  OR keep the terminal at the repo root. Do not `cd` into subdirs for shell commands
  that share state with the file tools; use `workdir=` on terminal instead.

## Disk
12Gi free on / (40% used). Healthy all tick.
