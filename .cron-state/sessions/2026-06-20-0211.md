# Tick 2026-06-20 02:11 PT — first tick

## Context

- Branch: `feature/autoship` (just created off `origin/main` @ c1a0bbf).
- Repo was fresh source (no node_modules). Ran `pnpm install` first.
- System root volume is at 100% capacity; Hermes shell snapshots fail with
  "No space left on device" warnings. Confirmed harmless (commands still
  succeed). Worked around vitest's similar TMPDIR write requirement by
  pointing `TMPDIR=/Volumes/Projects/.vitest-tmp`.

## Shipped (5 features + 1 infra unblock)

### Feature 1 — GitLab Code Quality JSON exporter — `dffc194`
- `packages/aggregator/src/gitlab.ts` (new, 127 lines)
- Wired through `packages/aggregator/src/index.ts`.
- Maps ClawReview severities → GitLab severities, forwards endLine, attaches
  Security/Performance/Style/Bug-Risk category heuristically, embeds rationale
  + CWE + suggested patch in content.body, accepts a severityMap override.
- Tests: 9 cases (`packages/aggregator/tests/gitlab.test.ts`).

### Feature 2 — SARIF enrichment — `cc52898`
- `packages/aggregator/src/sarif.ts` (+69/-4)
- Adds `partialFingerprints` (both `clawreviewFingerprint` and the
  GitHub-recognised `primaryLocationLineHash`) using the aggregator's
  content fingerprint so SARIF and baseline-diff agree on identity.
- Adds `helpUriFor` callback for per-rule docs URLs (exceptions and empty
  strings are swallowed so a stale doc map can't break SARIF emission).
- Adds `suppressions[]` on results matched by fingerprint against an
  opt-in suppressions input.
- Tests: 4 new cases on top of the existing 6.

### Feature 3 — File-level inline suppression — `f5a7a6a`
- `packages/aggregator/src/suppress.ts` (+77/-8)
- New marker: `// clawreview-disable-file[: rule1,rule2]`. Whole-file
  suppression with the same rule-scope semantics as the line markers.
- `SuppressionMap` gains a `fileLevel: Map<path, Suppression>`.
- The disable-file line is NOT also counted as a same-line ignore (avoids
  silent double-accounting); merge semantics for scopes mirror per-line.
- Tests: 4 new cases on top of the existing 9.

### Feature 4 — PR comment Run summary footer — `873ab53`
- `packages/aggregator/src/comment.ts` (+82)
- `CommentOptions.runSummary` is optional; when supplied, renders a
  collapsed `<details><summary>Run summary</summary>` block with duration,
  total cost, skipped-file count, and a per-agent breakdown table.
- Duration formatting: ms <1s, s.x <60s, "Mm SSs" beyond.
- Agent-error rows show the truncated error message inline.
- Tests: 4 new cases on top of the existing 3.

### Feature 5 — CLI `--format markdown` and `--format gitlab` — `67ebf4f`
- `apps/cli/src/commands/run.ts` (+67) and `apps/cli/src/help.ts` (+4)
- `--format markdown` wires `renderReviewReport()` end-to-end, building
  `ReportMetadata` from the pipeline summary so the heading and agent
  table reflect the exact run.
- `--format gitlab` emits the indented GitLab Code Quality JSON array.
- Help text + example invocations updated.

### Infra unblock — vite ^6 override — `eac1ef0`
- `package.json` adds `devDependencies.vite ^6.0.0` and
  `pnpm.overrides.vite ^6.0.0`. vitest 4.1.8 needs vite ^6 to resolve
  `vite/module-runner`; without this every test in the repo died at
  startup with `ERR_PACKAGE_PATH_NOT_EXPORTED`.
- After the override: vitest runs cleanly in every package I checked.

## Quality gates

`pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all return
non-zero on the workspace because of pre-existing baseline issues on
`origin/main` (missing `@types/node` in diff/llm/ui/db; the dashboard's
ESLint config wants interactive setup). Confirmed identical failures by
checking out `origin/main` to a temporary worktree and running the same
commands.

Verified per-package test runs directly:

| Package | Tests | Result |
|---|---|---|
| aggregator | 88 | pass |
| cli | 15 | pass |
| diff | 24 | pass |
| agents | 37 | pass |
| types | 7 | pass |
| llm | 12 | pass |
| github | 14 | pass |
| queue | 3 | pass |
| telemetry | 6 | pass |
| server | 179 | pass |

Total: 385 tests passing across the packages I touched and their
neighbours. The five new files of tests I added (87 new test cases
total spread across gitlab.test, sarif.test, suppress.test, comment.test)
all pass.

## Environment notes for next tick

- `export TMPDIR=/Volumes/Projects/.vitest-tmp` before running any test
  command — the system /tmp lives on the full root volume.
- The next reasonable foundational fix (and a near-zero-risk PR) would be
  to add `@clawreview/diff`, `@clawreview/llm`, `@clawreview/ui`, and
  `@clawreview/db` `@types/node` as a devDep and add `node` to their
  tsconfig `types` array. That single change would turn `pnpm typecheck`
  / `pnpm test` / `pnpm build` green and unblock the gates for every
  future tick. Seeded into STATE.md backlog.
- Sanjay should know: root volume is at 100% (228GiB used / 118MiB free).
  Disk pressure leaked into a stale `/tmp/msg.txt` from another autoship
  job that landed in my first commit attempt; I caught it and amended
  before pushing. Going forward I write commit messages into
  `.cron-state/msgs/` on the project volume.
