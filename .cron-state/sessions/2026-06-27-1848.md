# Tick 44 — 2026-06-27 18:48 PT (FRONTEND BATCH)

Frontend override active. Shipped 5 user-facing UI slices to `apps/dashboard/src/`, one commit each, gated once, pushed to main.

## Shipped

1. **Reviews-list findings above-baseline markers** (`57a14b0`) — neutral accent ring on the findings column for reviews that jump well past the page mean (ratio >=1.8x AND gap >=mean+3), mirroring the repo-trend-card "above baseline" idiom. Distinct from tick-40's orange spend-spike ring; completes the spend/findings outlier symmetry. Header gains an "N above baseline" legend. reviews/page.tsx.
2. **Command-palette `status:` comma list** (`b707ff6`) — `status:failed,running` ORs several statuses. parseStatusFilter returns a deduped string[]; pill renders a dot+label per status. 8-case node spot-check. command-palette.tsx.
3. **Trends sort axis URL persistence** (`b1ecc87`) — avg|total toggle reflects into `?sort=` (shallow replaceState, client-side sort = no re-fetch), seeded server-side. WindowForm reads live location.search so a window change keeps a non-default sort. trends/page.tsx, agent-performance-table.tsx, window-form.tsx.
4. **Bulk dismiss undo toast** (`5134d15`) — the bulk-dismiss success toast carries an "undo" that reopens the same filter via bulkReopenAction. Reuses tick-43 ToastAction. bulk-findings-bar.tsx.
5. **Toast keyboard control** (`3327894`) — global `u` fires the most-recent actionable toast's undo + dismisses; `Escape` clears the corner. Ref-mirrored state so the once-registered handler stays fresh; skipped while typing. Undo button gains a `u` kbd badge. toaster.tsx.

## Gate

- dashboard `tsc --noEmit`: IDENTICAL to baseline (5 lines, only the pre-existing TS5101 baseUrl deprecation; 0 non-baseline errors). Verified per-slice + after final commit.
- `next build`: production bundle **Compiled successfully in 8.5s** (all 5 slices green). Residual failure is ONLY Next's separate standalone type-check phase on `layout.tsx:5 import './globals.css'` — proven pre-existing + untouched (layout.tsx not in batch diff; failing line byte-identical origin/main vs HEAD). Same documented quirk as ticks 31/33-43.
- Scope: 7 files, +204/-37, all under `apps/dashboard/src/`; zero backend/packages touched.
- Push verified: `git log --oneline origin/main | head -1` -> `3327894`. HEAD == origin/main.

## Decision: retired the 14-tick hover-preview item

Formally RETIRED "reviews list per-row hover preview" — carried unshipped since tick 30 (14 ticks). It needs a `bySeverity` field on the ReviewListItem server DTO + the list endpoint's data layer, which can't be integration-tested in a night cron tick (no running server to verify the wire shape end-to-end). The backlog itself flagged 14-ticks-unshipped as a smell. Chose 5 clean verifiable slices over forcing a 6th that can't be gated.

## Notes

- Disk healthy: 15Gi free on /, 45% used. patch tool worked fine all tick.
- No dashboard test files exist, so the parseStatusFilter signature widening (string -> string[]) is safe; spot-checked the parser with a standalone node script instead.
- Parked backend roadmap (worker blame, dashboard-for-counters, Grafana JSON, presets/filter-report hooks) stays parked under the override until Sanjay flips it back.
- Refilled tick-45 backlog with 10 frontend-first items (findings windowed paint, SLA age brightening, palette help-row + multi-select chips + status legend, toast u-walk-back, combined outlier legend, sort round-trip verification, overview reliability/SLA parity).
