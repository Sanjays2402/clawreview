# Tick 30 — 2026-06-25 00:51 PT — FRONTEND BATCH (5 features)

Standing frontend override active (Sanjay, 2026-06-23): all 5 slices are
user-facing UI work in `apps/dashboard/src/`. Backend roadmap parked.

## Shipped (5 slices, one commit each, all on origin/main)

1. **43f61ea** — Reusable `useListKeyboardNav` hook + reviews-list j/k/gg/G/Enter nav
   - `src/lib/use-list-keyboard-nav.ts` (new): generalised the findings-list
     j/k/gg/G handler; selector + enabled opts; matches el OR el.contains(active)
     so a focusable child inside the row counts; ignores INPUT/TEXTAREA/SELECT/CE.
   - `src/components/list-keyboard-nav.tsx` (new): headless mount so RSC pages opt in.
   - reviews page: PR-cell link is now `[data-review-row]` with focus-visible ring,
     row `focus-within` tint, PageHeader keyboard hint. Shortcuts overlay updated
     with a shared "lists (reviews / findings)" group.

2. **696b2f5** — Persist findings file-group expand state per review
   - `usePersistentExpand(reviewId, files)` in findings-group.tsx: localStorage key
     `clawreview-findings-expand:<reviewId>`. SSR-safe (server defaults paint first,
     post-mount effect overlays overrides), only user toggles stored, stale keys
     pruned on load, accent dot marks remembered groups.

3. **28205db** — InteractiveSparkline on overview findings/day
   - `src/components/charts/interactive-sparkline.tsx` (new): per-bucket invisible
     hit-targets, highlighted dot + dashed vertical guide, floating mono readout
     (count + unit + "today/2d ago" label) that flips side near the right edge,
     accent gradient area-fill, keyboard cursor (Left/Right/Home/End/Escape).
   - overview rewired off the static `<Sparkline>` (primitive kept for decoration).

4. **751b6ad** — Repos page status tabs + sortable columns + filter chips
   - tabs all/healthy/degraded/paused with live count badges, sortable
     repository/failures/last-review (aria-sort + arrow, per-column default dir),
     active-filter chips with per-chip remove + clear-all, restyled to the dense
     lowercase/mono/sev design. Pure server-component URL state.

5. **0f09de4** — Copy-deep-link + focus-scroll on findings
   - finding-row: hover-revealed Tooltip-wrapped link-copy button -> copies
     `/app/reviews/:id/findings?focus=<id>` with check-mark confirm; `focus` prop
     scroll-to-center + focus + inset accent ring; row id `finding-<id>`.
   - findings page reads `?focus=`, keeps the target past active filters,
     force-opens its file group (findings-group threads `focusId`).

## Gate

- Dashboard `tsc --noEmit` output IDENTICAL to tick-29 baseline: only the
  pre-existing TS5101 `baseUrl` deprecation (tsconfig.json:18). Zero new type
  errors. Verified post-batch (immediately after slice 5, ~10s, 5-line output)
  AND via a confirmatory background tsc after push (new `error TS` count = 0).
- `next build` did NOT complete: `next/font/google` (Inter_Tight + JetBrains_Mono)
  fetches from Google's CDN at build time and the sandbox stalls that call at the
  font-load stage before any page compiles (proc at 0.0% CPU). Environmental, same
  class as tick-29's documented "no full build / no UI unit-test infra" limitation.
  The gate is typecheck-baseline-parity + scoped-diff review.
- Mid-tick the host entered heavy EXTERNAL memory thrash (swap pinned ~9840M from
  Firefox/gopls, not my procs), which made some tsc runs sit on swap I/O
  (0.78s CPU over 2m30s wall) — proven non-code; all my heavy procs were killed.

## Scope

- 10 files, +701/-69, ALL under `apps/dashboard/`. Zero backend/packages touched.
- Push verified: origin/main HEAD -> 0f09de4.

## Notes for next tick
- Theme bootstrap (a tick-30 backlog item) was ALREADY shipped via root-layout
  `ThemeBoot` — verified, no work needed. Marked done in STATE.
- New reusable primitives available to reuse: `useListKeyboardNav` /
  `ListKeyboardNav` (any dense table), `InteractiveSparkline` (any series),
  the sortable-th + chips pattern is now in BOTH reviews and repos.
- FindingDto has no `fingerprint` field; deep link uses the stable `id`.
- 5 backlog items carried to tick 31 + 5 fresh frontend items seeded.
