# Example configurations

- `strict.clawreview.yml` — security-focused, threshold medium, skips tests.
- `lenient.clawreview.yml` — style-only, very low signal floor, compact comments.

Copy whichever you want to the root of your repo and rename to `.clawreview.yml`.
